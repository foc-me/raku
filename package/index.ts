import { is } from "./is"
import { classname } from "./classname"
import { compare, createCompare } from "./compare"
import { createCurrent } from "./current"
import { createController, provideEmitter, createEmitter } from "./emitter"
import { createTimeout } from "./timeout"

export {
    is,
    classname,
    compare,
    createCompare,
    createCurrent,
    createController,
    provideEmitter,
    createEmitter,
    createTimeout,
}

const raku = {
    is,
    classname,
    compare,
    createCompare,
    createCurrent,
    createController,
    provideEmitter,
    createEmitter,
    createTimeout,
}

export default raku